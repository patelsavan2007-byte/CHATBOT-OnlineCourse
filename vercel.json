{
  "installCommand": "npm install --prefix deployment",
  "buildCommand": "npm run build --prefix deployment",
  "outputDirectory": "deployment/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
